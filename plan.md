# План реализации "Задания 3"

## Этап первый: Инфраструктура и конфигурация

- Создаем .env файл Записать в него параметры для базы данных (POSTGRES_USER, POSTGRES_PASSWORD,
DATABASE_URL и т.д.).
- Реализуем класс для чтения переменных из .env 
- Реализуем скрипт для управления движком и сессиями в базе данных
- Описываем сервисы (SQL и FastAPI) в docker-compose.yml
- Реализуем Dockerfile для сборки приложения
- Проверяем реализацию заданного функционала

## Этап второй: 'Модели и Схемы'
- Реализуем классы для описания групп и студентов, учитывая поля в базе данных
- Реализуем Pydantic модели для запросов

## Этап третий: 'Бизнес-логика'
- Реализуем скрипт crud.py для управления групами и студентами в таблицах базы данных:
        1. create_group / create_student 
        2. get_group / get_student 
        3. delete_group / delete_student
        4. get_all_groups / get_all_students
        5. add_student_to_group / remove_student_from_group
        6. get_students_by_group/ move_student_to_group
        7. Реализуем функции для отслеживания кол-ва участников в группах

## Этап четвертый: API - маршрутизация
- Реализуем routers/students.py: создаем эндпоинты для студентов, используя APIRouter
- Реализуем routers/groups.py: Создаем эндпоинты для групп, используя APIRouter.

## Реализуем запуск и сборку
- Проверяем и тестируем сборку
- main.py: 






